<Window x:Class="Chess.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chess"
        mc:Ignorable="d"
        Title="Chess" Width="900" Height="750"
        Icon="pics/icon.ico"
        WindowStartupLocation="CenterScreen"
        Background="#222">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="650"/>
            <!-- Viewbox with game UI -->
            <ColumnDefinition Width="*"/>
            <!-- Move log panel -->
        </Grid.ColumnDefinitions>

        <!-- Layout wrapped in a Viewbox -->
        <Viewbox Grid.Column="0" Stretch="Uniform">
            <Grid x:Name="MainGrid" Height="697" Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label x:Name="TurnLabel" Content="White's Turn" 
                       HorizontalAlignment="Center" VerticalAlignment="Top" 
                       FontSize="24" Foreground="White" Margin="0,10,0,0"
                       Grid.Row="0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1">
                    <Label x:Name="WhiteTimerLabel" Content="White: 00:00"
                           FontSize="20" Foreground="White" Margin="0,5"/>
                    <Label x:Name="BlackTimerLabel" Content="Black: 00:00"
                           FontSize="20" Foreground="White" Margin="20,5,0,5"/>
                </StackPanel>

                <Grid x:Name="PieceGrid"
                      Grid.Row="2"
                      Width="600"
                      Height="600"
                      Focusable="True">
                    <Grid.Background>
                        <ImageBrush ImageSource="pics/Board.png"/>
                    </Grid.Background>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                </Grid>

            </Grid>
        </Viewbox>

        <!-- Move Log Panel -->
        <Border Grid.Column="1" Background="#222" Padding="10">
            <ListView x:Name="MoveLogView"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Margin="0,10,10,10"
                Background="Black"
                Foreground="White"
                BorderBrush="White"
                FontSize="16">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="#" Width="30" DisplayMemberBinding="{Binding Turn}" />
                        <GridViewColumn Header="White" Width="85" DisplayMemberBinding="{Binding WhiteMove}" />
                        <GridViewColumn Header="Black" Width="85" DisplayMemberBinding="{Binding BlackMove}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</Window>
